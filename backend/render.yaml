services:
  - type: web
    name: ielts-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DOUBAO_API_KEY
        sync: false
      - key: DOUBAO_ENDPOINT
        sync: false
